# Cody's Blog

## Usage
This blog uses Hexo

	npm install
	hexo s -w

It also uses pandoc

	sudo apt-get install pandoc

### To start the server

	`hexo server`

### To write a new post

	`hexo new "POSTNAME"`