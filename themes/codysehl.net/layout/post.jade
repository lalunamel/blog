extends default-layout

block content
  .blog.single-post-page
    include _partials/sidebar
    .sidebar-wrap
      .single-post
        h2.header #{page.title}
        h3.date #{page.date.format('MMMM Do, YYYY')}
        .content!= page.content